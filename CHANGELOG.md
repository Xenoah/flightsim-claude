# CHANGELOG

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)、
バージョンは [Semantic Versioning](https://semver.org/lang/ja/) に従う。

---

## [Unreleased]

### 追加

- **`flightsim-tilegen`** — Copernicus DEM の GeoTIFF から実行時タイルを焼く
  オフライン CLI。EPSG:4326 の単バンド浮動小数点ラスタを読み、地理座標系
  クアッドツリーのタイルへリサンプリングして書き出す
  - 足跡が元画素より粗い場合は**面積平均**、細かい場合はバイリニア補間。
    粗い側で点サンプリングするとエイリアシングが幾何誤差として算出され、
    平野が過剰に細分化される
  - `GTRasterTypeGeoKey` を見て `PixelIsArea` / `PixelIsPoint` を切り替える。
    取り違えると 30 m データで 15 m ずれる
  - 投影座標系のラスタは**度として読まずにエラーにする**。静かに全く違う場所の
    地形になるため
  - 日付変更線をまたぐ範囲（`west > east`）と極を扱う
  - nodata 画素を重みから外し、海岸線が虫食いにならないようにする
- **実行時タイル形式 `.fsdem`**（[ADR-0005](docs/adr/0005-runtime-tile-format.md)）を
  `flightsim-world::dem::io` に実装。`u16` 量子化 + タイル毎スケールで、
  標高差 4 000 m の山岳でも約 6 cm 分解能
  - フォーマット版とペイロードの FNV-1a チェックサムを持つ。
    **壊れたファイル・切り詰め・版違いで panic せずエラーを返す**
  - 幾何誤差をヘッダに埋め、LOD 選択が画素を読まずに判断できる

### 修正

- `scripts/check-architecture.sh` が **`cargo tree` の失敗を握り潰していた**。
  マニフェストに誤りがあると全ての規約検査が「依存ゼロ」として黙って通り、
  安全網が無言で外れる状態だった。事前検査を足して即座に落とすようにした
  （実際にこの穴を踏んで気付いた）

### 検証

297 件のテストが通る。実行時タイル形式については、TIFF 6.0 と ADR-0005 の仕様書だけを
見て Python で独立に writer / reader を書き、Rust 実装と突き合わせた。
同じ実装者の writer と reader を往復させても共通の誤解は検出できないため。

### 既知の制限

- **被覆外を埋めた部分が幾何誤差を押し上げる。** 実データと fill の段差が崖として
  扱われ、実測で被覆完全なタイル最大 10.7 m に対し fill を含むタイルは最大 375.3 m
  になった。実データが無い場所ほど細分化されるという逆転が起きる。
  当面は `--bounds` で被覆内に生成を限定する。将来はフェザリングか、
  被覆率の低いタイルの除外で対処する
- タイル生成は標高のみ。OSM（空港・建物）と地表画像は未対応
- ダウンロード機能は無い。GeoTIFF は手元に用意する必要がある

---

## [0.2.0-alpha.1] — 2026-08-03

### 追加

- `flightsim-fdm` に前脚 1 本・主脚 2 本の着陸装置を追加。ばねダンパ式の接地反力、
  最大ストローク後のバンプストップ、転がり・横滑り摩擦、ホイールブレーキに対応
- 呼び出し側が `Environment` へ地面の楕円体高とローカル勾配を渡す接地平面入力
- 脚の固有振動数を考慮する内部サブステップ判定
- 10 分静止、理論沈み込み、滑走離陸、3 m/s・10 m/s 着地、傾斜面、決定論の回帰テスト

---

## [0.1.0] — 2026-08-01

**M1: 物理と地形の基盤。**

最初のリリース。地球規模フライトシミュレータの土台にあたる部分を、
テストで検証できる状態まで作ったもの。

> **これはまだゲームではありません。** 描画が一切なく、実行できるのは
> テストと診断用の example のみです。何が動いて何が動かないかは
> [ARCHITECTURE.md §7](ARCHITECTURE.md#7-現状のスコープ) を参照してください。

### 追加 — `flightsim-core`

座標系・単位の共通基盤。他のいかなるクレートにも依存しない。

- 単位付き newtype（`Meters` / `Knots` / `Radians` / `Kelvin` ほか）。
  ft・kt・deg の取り違えを型で潰す
- WGS84 測地系と ECEF 変換。ECEF → 測地は Bowring 法 + 固定点反復で、
  地表付近 0.1mm 以下の精度
- ローカル接平面（NED / ENU）と、3-2-1 オイラー角による姿勢。
  ピッチ ±90°（垂直上昇・降下）のジンバルロックを処理する
- Floating origin。`f64` ECEF から描画用 `f32` への変換と、アンカーの打ち直し
- 固定タイムステップのアキュムレータ（death spiral 防止のクランプ付き）

### 追加 — `flightsim-fdm`

6 自由度の飛行力学モデル。決定論的で、Bevy にもワールドにも依存しない。

- ISA 標準大気（幾何高度 -5km〜86km）。ジオポテンシャル高度への換算と、
  非標準日の温度偏差に対応
- WGS84 正規重力（Somigliana の式）。緯度と高度による変化を含む
- 空力係数モデル。失速角の前後で線形モデルと平板理論をシグモイドでブレンドし、
  失速後に揚力が落ち抗力が急増する挙動を再現
- RK4 固定ステップ積分。角速度に応じた内部サブステップ分割付き
- 定出力プロペラモデル（静止推力の上限付き）

### 追加 — `flightsim-world`

地形タイル・LOD・ストリーミング。GPU には触れない。

- 地理座標系クアッドツリー（Cesium geographic tiling scheme 互換）。
  Web メルカトルと違い極まで表現できる
- DEM のバイリニアサンプリングと、実データから算出する幾何誤差
- 幾何誤差ベースの screen-space error による LOD 選択。距離ベースではない
- 距離優先の読み込みキューと、バイト数上限つき LRU キャッシュ。
  1 フレームあたりの読み込み数に上限を設けフレームスパイクを防ぐ

### 追加 — 開発基盤

- 意思決定ログ [ADR-0001〜0004](docs/adr/)。エンジン選定、座標系、
  地形データ方針、シミュレーションループの根拠とトレードオフ
- 8 つの専門エージェント定義（[.claude/agents/](.claude/agents/)）
- CI: `cargo test` / `clippy -D warnings` / `fmt --check` / `doc -D warnings`、
  および[依存規約の機械検査](scripts/check-architecture.sh)
- 診断用 example [`aero_trace`](crates/flightsim-fdm/examples/aero_trace.rs)

### 検証

202 件のテスト（ユニット 182、統合 17、doctest 3）が通る。
`core` / `fdm` / `world` は Bevy 非依存のため、GUI もアセットもなしに数秒で完了する。

外部の公表値との照合を重視している。ISA 標準大気の表、WGS84 の定義値と正規重力の
公表値、国際フィート・海里の定義値など。「実装がこう返すから正しい」というテストは
検証にならないため。

### 含まれないもの

- **描画**。大気散乱・地形メッシュ・HUD いずれも未実装
- **実地形データの読み込み**。タイル生成パイプラインが無いため、
  テストは全て合成データで動いている
- 着陸装置と接地反力。したがって離着陸はできない
- FDM とワールドの結線。個別には動くが、地形標高はまだ FDM に渡されていない
- 天候、ライブ交通、オンライン共有ワールド、複数機体、コックピット操作

### 既知の制限

- **コリオリ力・遠心力を無視している。** 巡航速度 250 m/s・緯度 45° で
  重力の約 0.26%。長距離巡航の航法誤差として効くため、
  [ADR-0002](docs/adr/0002-coordinate-system.md) に将来の対応方針を記録してある
- 空力係数は軽single機の代表値であり、**特定の実在機の認証データではない**
- 推力線オフセットによるピッチモーメントが未実装
- 高度は楕円体高であり、ジオイド高（平均海面基準）ではない。
  両者は場所により最大 100m 程度ずれる
- ベンチマークが未整備。性能について測定に基づく主張ができない

[Unreleased]: https://github.com/Xenoah/flightsim-claude/compare/v0.2.0-alpha.1...HEAD
[0.2.0-alpha.1]: https://github.com/Xenoah/flightsim-claude/compare/v0.1.0...v0.2.0-alpha.1
[0.1.0]: https://github.com/Xenoah/flightsim-claude/releases/tag/v0.1.0
