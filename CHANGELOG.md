# CHANGELOG

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)、
バージョンは [Semantic Versioning](https://semver.org/lang/ja/) に従う。

---

## [Unreleased]

---

## [0.6.0-alpha.1] — 2026-08-24

**M2 達成: 滑走路から離陸して、戻って降りると、着陸が評価される。**
ゲームとして一周する最初の版。

### 追加 — 合成飛行場

- `flightsim-world` に `Runway`（threshold・方位・長さ・幅・標高、
  contains / 前方距離・横ずれ / takeoff_start）。合成 DEM と対のフィクスチャ
- 滑走路の描画: 舗装・中心線の破線（実規格 30 m + 20 m）・ピアノキー。
  頂点は接平面ではなく測地座標で置く（接平面だと両端が曲率で 12 cm 沈む）。
  巻き順テストが裏返り（背面カリングで滑走路が消える）を絵を出す前に捕まえた
- 既定の開始位置を滑走路の離陸開始点へ。**以前の既定は滑走路から横に
  75 m 外れていた**
- `Geodetic::offset_by`（北・東メートルの局所オフセット）を core に追加。
  測量既知値（赤道の緯度 1° = 110 574 m）と突き合わせ

### 追加 — 着陸評価

- `Simulation` が接地の瞬間（接地点・沈下率・対地速度・バンク・方位）を記録。
  **接地直前の空中の状態から取る**（接地後では脚のばねが衝撃を吸収して
  実際より穏やかに見える）。ヒステリシスでバウンドの二重カウントを防ぐ
- HUD 右上に 5 段階評価（Greased 〜 Dangerous）＋数値（ft/min・kt）。10 秒で消える。
  滑走路外は Normal 止まり、バンク 5° 超は 1 段階降格
- 検証用 `--drop <高さ m>`: 空中から落として接地させ、記録 → 評価 → 表示の
  経路を通す。実際に「Dangerous / 1312 ft/min / On runway」の表示を確認した

### 追加 — ゲームパッド

- 左スティック = 操縦桿（手前で機首上げ）、右スティック X = ラダー、
  RT/LT = スロットル増減（**保持式**。実機のスロットルレバーと同じ）、
  十字キー = フラップ、A = ブレーキ、Y = 視点切替
- デッドゾーン 0.1・感度カーブつき。デッドゾーン境界で入力が跳ねない
  （連続に 0 から立ち上がる）ことをテストで固定
- **キーボードと軸ごとに共存**。触れている軸だけゲームパッドが取り、
  残りはキーボードのまま。未接続ならキーボード経路と完全一致することを検査

### 修正 — 傾斜地の spawn で機体が裏返っていた

2 段階で直した。どちらも「めり込み or 落下のエネルギーが転倒障壁
2.9 kJ を超える」が本質。

1. **fdm: 接地反力に上界を導入。** めり込み 0.43 m の線形ばねは 20 kJ を
   蓄え、機体を背面まで一回転させていた。バンプストップの行程（0.05 m）で
   弾性力を頭打ちにし、脚の伸長速度に上限（0.5 m/s）を設けた。接地が
   注入できるエネルギーは数百 J で有界になり、深さに依存しない。
   着陸の最大圧縮は沈下率 1〜10 m/s の全域で修正前と一致
2. **sim: 駐機の姿勢を斜面に合わせる。** 水平姿勢では、低く置けば
   めり込み（20 kJ）、高く置けば落下（25° で 7.6 kJ）でどちらも裏返る。
   **後者は自分の中間修正が作った退行で、CI が捕まえた。** 実機と同じく
   機体を斜面に沿って傾け、全脚を同時に接地させる

### 検証

M2 完了条件を受け入れテストとして固定（`airport_circuit`）: 合成飛行場から
離陸 → 旋回 → 着陸、全フェーズ通過・空港 10 km 圏内・静止・NaN 不在。
テスト合計: 純 Rust 側 511 件、描画層 89 件。

### 既知の制限

- 自動操縦は滑走路へ戻る横方向誘導を持たない（M3）。滑走路上への
  精密着陸はプレイヤーの操縦で行う
- ゲームパッドは実機未検証（テストのみ）。実パッドでの符号・感度の
  確認は次のプレイテストで
- HOTAS・軸の再バインド UI は未対応

---

## [0.5.0-alpha.5] — 2026-08-23

**地表が標高と傾斜で塗り分けられるようになった。**
そのために、まず**地形を目で見られるようにした。**

### 追加 — 合成 DEM

```bash
cargo run -p flightsim-tilegen --example synthetic_dem -- data/synthetic.tif
cargo run -p flightsim-tilegen -- --input data/synthetic.tif --output data/tiles \
    --min-level 8 --max-level 12
cargo run -p flightsim-app --release -- --tiles data/tiles --start 35.55,139.45
```

実 DEM は 1 タイルでも数十 MB あり、リポジトリにも CI にも置けない。結果として
**地形が映らないまま描画コードを書く**ことになる。glTF 経路ではそれで 3 件見落とした。

**実在しない地形**であり、実データの代わりにはならない。投影のずれ・nodata・
データ境界の崖といった実 DEM の面倒は現れない。見えるのは、地形が出るか・
LOD が切り替わるか・色や陰影が妥当か。

### 追加 — 標高と傾斜による塗り分け

**衛星画像ではない。** 実際の土地被覆（市街地・森林・農地）は反映されない。
画像を貼るのは、データ源とその権利を決めてからの別の作業。

- 低地は緑、上がるにつれて褐色、さらに上は岩と雪
- **急斜面は高度によらず岩肌。** 高度だけで塗ると崖に草が生える
- 単色では起伏が読めず、陰影だけでは太陽の向きに依存して**逆光で真っ平らに見える**
- `TerrainMesh` が頂点ごとの標高と傾斜を持つようにした。生成時には手元にある値
  なので、描画側で ECEF から測地座標へ逆変換し直さずに済む

### 修正 — 頂点色を sRGB のまま渡していた

`Mesh::ATTRIBUTE_COLOR` は**線形 RGB** として扱われる。`Color::srgb` の感覚で
選んだ数値をそのまま渡していたため、同じ地点・同じ光で画面の色が
(0.328, 0.347, 0.229) から (0.509, 0.521, 0.374) へ明るく浅くなった。

**目視では「そんなものか」で済んでしまい、画素を測って初めて分かった。**
`terrain_color` は sRGB を返し、`terrain_colors` が線形へ変換する。

### 検証

検査を 12 件追加（純 Rust 側 460、描画層 81）。合成地形を実際に描画し、
**画素の平均値を測って**修正前後を突き合わせた。

### 既知の問題

- **急斜面に spawn すると機体が裏返る。** 山腹（傾斜のきつい地点）を `--start` に
  指定すると、接地状態のまま姿勢が崩れる。接地反力の問題で、塗り分けとは別件
- 雪線を緯度で変えていない。合成地形でしか確かめていないため、根拠のない
  精緻化はしていない

---

## [0.5.0-alpha.4] — 2026-08-23

**引数なしで起動すれば、テクスチャ付きの機体が出るようになった。**
そのために起動経路を実際に 3 通り試したところ、**さらに 2 件壊れていた。**

### 追加 — 機体モデルを同梱した

`assets/aircraft/light_single.glb`（4.75 MB）をリポジトリに含めた。有料プランで
生成しており再配布可（[ATTRIBUTION.md](ATTRIBUTION.md) に記録）。

- 引数なしの既定を同梱モデルにした。軸は**同梱ぶんの実測値**（前 = −X、上 = +Y）
- **同梱モデルの軸を全体の既定にしていない。** そうすると他所から持ってきた
  モデルまで −X 前方として扱われ、横を向いた理由が分からなくなる。
  `--model` を指定した場合の既定は glTF の慣習（−Z 前方）のまま
- `--no-model` で箱のプレースホルダに戻せる
- 指定したモデルが実在しない場合はプレースホルダへ落とし、**探した場所を言う**
- **コックピット視点では外形を隠す。** 目線が胴体の内側に入るので、そのまま描くと
  視界が自分の機体で塞がる。**既定の視点はコックピットなので、引数なしで起動
  したときに最初に見える画がこれだった。** 内装モデルはまだ無い

### 修正 — 文書の起動コマンドが動いていなかった

`cargo run -p flightsim-app` では `CARGO_MANIFEST_DIR` が `crates/flightsim-app` に
なり、そこに `assets/` は無い。**README と HANDOFF に書いてあった起動方法は
一度も動いていなかった**（検証時に `BEVY_ASSET_ROOT` を付けていたので気付かなかった）。

`assets_directory()` が候補から上へ辿って実体を見つけ、`AssetPlugin::file_path` に
絶対パスで渡すようにした。`cargo run` / 実行ファイル直叩き / 無関係な
ディレクトリからの起動、**3 通りすべてで同じアセットを解決することを確認した。**

### 修正 — 引数の誤りが無言で捨てられていた

`parse_arguments` は `App::new()` より前に走るので、そこでの `warn!` は購読者が
居らず**何も出ない**。`--bogus-flag` を渡しても無言だった。指摘を溜めて、
`Startup` スケジュールから出すようにした。

### 検証

`flightsim-app` に検査を 8 件追加した（これまで 0 件）。
**「同梱モデルの軸が他モデルへ漏れない」検査は、意図的に壊して落ちることを確認済み。**

---

## [0.5.0-alpha.3] — 2026-08-21

**Meshy から取得した実際の `.glb` を読み込んで飛ばせるようになった。**
0.5.0-alpha.1 で「glTF 経路は実データ未検証」と書いていた箇所で、
**実際に 2 件壊れていた。** どちらもテスト・clippy・CI が全て緑のままだった。

### 修正 — glTF シーンの生成で panic していた

`reflect_auto_register` feature が抜けていた。Bevy 0.18 は反射の型登録を
自動化しており、`GltfPlugin` は `register_type` を一度も呼ばない。切れていると
`scene contains the unregistered type` で落ちる。

**ビルドでもテストでも検出できない。** コンパイルも clippy も描画層のテストも
通り、CI も緑のまま（GPU が無いので描画を実行しない）。詳細は
[ADR-0007](docs/adr/0007-bevy-version.md)。

### 修正 — 倍率が機首方位に依存していた

寸法を描画フレームの軸で測っていたため、得られるのは**回転後の箱を包む箱**で、
同じモデルが方位 0° で 4.37 倍、50° で 3.70 倍、90° で 6.23 倍になっていた。

- 測定を `flightsim_render::extents_in_model_space` として純関数に切り出した
- `ModelFit::scale_for` の doc に、モデル座標系の寸法を渡すことを明記した
- 回帰検査を 4 件追加。**旧実装を注入して落ちることを確認済み**で、
  注入時の測定値 2.2430766 は実機ログの 2.2431（倍率 3.7004）と一致する
- 修正後は方位 0° / 50° / 90° のいずれでも 6.2288 倍

### 検証 — 実データで通した

Meshy から取得した `.glb`（1.1 MB、頂点 29,327、preview 段階につき素材なし）を
外部視点で描画し、姿勢と大きさを目視で確認した。

頂点分布からモデルの軸を割り出した: 前 = **−X**、上 = **+Y**、翼幅は Z に 1.901
（+Z 端の断面が 0.148 × 0.07 = 翼端、上端 25% の重心が +X 側 = 垂直尾翼）。
glTF の慣習（−Z 前方）とは違うため、**`ModelFit` の補正層が無ければ機体は
真横を向いていた。**

### 修正 — テクスチャ付きモデルが読み込めなかった

`jpeg` feature が抜けていた。Meshy はベースカラーを JPEG で返す。
**テクスチャが抜けるのではなく、glTF の読み込みが丸ごと失敗する。**

`png` だけ入れていたのはスクリーンショットの保存に要ったからで、
**書き出す形式と読み込む形式は別物**だった。

### 検証 — テクスチャ付きでも通した

`--refine` で素材を付けたモデル（4.75 MB、ベースカラー JPEG 1 枚）を描画し、
preview 版との画素差で素材の適用を確認した（機体領域の 5.2 %、最大差 118）。
拡大すると**赤い航法灯**（尾翼上端・翼付け根）と**パネルラインの筋**が乗っている。
倍率は preview と同一の 6.2288。

### 既知の制限

- 生成物はプロンプトどおりではない。高翼機を指示したが、翼は高さ帯 4〜5
  （中央）にあり中翼機になっている
- 法線マップは無い。ベースカラーのみ
- モデルは `assets/aircraft/` に置くが、**権利の扱いを確認するまでコミット
  しない**（`.gitignore` 対象。[ATTRIBUTION.md](ATTRIBUTION.md) を参照）

---

## [0.5.0-alpha.2] — 2026-08-21

**API キーを `.env` から読めるようにした。** 機能追加ではなく、鍵が読めない
ときに詰まらないようにするための変更。

### 変更 — `flightsim-assetgen` のキー取得

- **リポジトリ直下の `.env` から読む。** 同名の環境変数があればそちらが優先
  される（一時的に別の鍵で試すときにファイルを書き換えずに済ませるため）。
  **引数では受け取らない**のは従来どおり。書式は追跡している `.env.example` を参照
- 環境変数だけで渡す設計は、**現在のシェルで設定しても新しく起動したプロセス
  からは見えず**、「設定したのに読まれない」で詰まる。実際に詰まった
- `.env` は上位ディレクトリへ辿って探す。**見つからなければ探した場所を列挙する**
  ので、置き場所が違うのか綴りが違うのかを切り分けられる
- 鍵はあるが目的の名前が無い場合、**入っている名前だけ**を出す。値は出さない
- 解釈できなかった行は黙って捨てず、行番号を警告する。dotenv 系のクレートは
  黙って無視する挙動が多く、いちばん詰まりやすい状況で手掛かりが出ない
- `EnvFile` の `Debug` は名前だけを出す。`derive` のままだと、うっかり `dbg!`
  した瞬間に鍵がターミナルとログに残る

### 検証

`.env` 解釈のテスト 13 件を追加（CRLF、BOM、`export` 前置、値に含まれる `=`、
片方だけの引用符、値が `Debug` に出ないこと）。実バイナリで両経路を確認した
——鍵が無いときの案内と、サブディレクトリから上位の `.env` を見つける動作。

### 既知の制限

**実際のモデルを Meshy から取得した確認はまだ取れていない。** glTF 読み込み
経路は合成データでしか通していない。

---

## [0.5.0-alpha.1] — 2026-08-07

**外部視点で機体が映るようになった。**

### 追加 — 機体の見た目

- 箱を組み合わせたプレースホルダ機体。**寸法は `AircraftConfig` から引く**ので、
  翼幅を変えれば見た目も変わる。物理と見た目が食い違わない
- パーツは機体軸（X = 前、Y = 右、Z = **下**）で置く。Bevy の Y-up の感覚で
  組むと尾翼が下に生えるため、垂直尾翼の向きをテストで検査している
- `--view cockpit|chase|free|tower` で起動時の視点を選べる

### 追加 — glTF モデル

- `ModelFit`。外部モデルの座標系（前・上の軸）を機体軸へ合わせる補正層。
  **モデルを差し替えるたびに描画コードを書き換えないための層**
- 倍率はモデルの寸法から自動で決める。glTF は非同期に読み込まれるので、
  子のメッシュが揃った時点で AABB を測る
- 寸法が取れない場合は等倍。0 除算で倍率が無限大になると、機体が画面を
  埋め尽くして原因が分からなくなる
- ADR-0007 で「必要になった時点で足す」としていた `bevy_gltf` を有効にした

### 追加 — `flightsim-assetgen`

Meshy から機体モデルを取得するオフライン CLI。

- **API キーは `MESHY_API_KEY` 環境変数からのみ読む。** 引数では受け取らない
  （コマンドラインはプロセス一覧とシェル履歴に残る）
- ログとエラーではキーを伏せる。先頭 4 文字だけ残す
- **キーが無くても起動でき、何をすればよいかを言って終わる**
- 要求も完了待ちも上限つき。連続 5 回の失敗で打ち切る
- **未知の状態を「成功」にも「失敗」にも倒さない**
- 要求の組み立てと応答の解釈は HTTP から切り離してあり、キーもネットワークも
  無しでテストできる

### 検証

489 件のテスト（純 Rust 側 442、描画層 47）。

**`cargo test --workspace` はこのマシンでは通らない。** Bevy を含む全クレートの
テストバイナリを同時にビルドするとメモリを使い切り、
`failed to mmap ... The paging file is too small`（os error 1455）で落ちる。
コードの問題に見えるが環境の問題。CI と同じ 2 グループに分けて回すこと。

### 既知の制限

- **glTF 経路はまだ実モデルで検証していない。** `ModelFit` の数学は単体テストで
  検査しているが、実際の `.glb` を読んで表示するところまでは通していない
- 生成モデルの権利はプランに依存するため `assets/aircraft/` はコミットしない
- ゲームパッド／HOTAS 未対応、地表テクスチャなし、衝突判定なし

---

---

## [0.4.0-alpha.1] — 2026-08-07

**M2: 画が出る。** 実地形の上を飛ぶ様子が画面に表示される。

### 追加 — Bevy 描画層

[ADR-0007](docs/adr/0007-bevy-version.md) で **Bevy 0.18.1** に確定。最新は 0.19.0 だが
MSRV 1.95 で現行ツールチェーン（1.93）では cargo が解決すらしない。
**机上で決めず、実際にコンパイルが通ることを確かめてから選定した。**

- `flightsim-render` — floating origin の適用、地形メッシュの GPU 投入、
  LOD ストリーミング（フレーム予算つき）
- `flightsim-input` — キーボード操縦。**舵はレート制御 + 中立復帰**
  （on/off をそのまま舵角にすると制御不能になる）。視点 4 モード
- `flightsim-ui` — HUD。単位変換は `flightsim-core` 経由のみ。
  数値は 0.1 秒間隔で更新（毎フレーム更新すると下 1 桁が読めない）
- `flightsim-app` — 統合バイナリ。`--screenshot` で目視確認用に 1 枚撮れる

### 追加 — 純 Rust 側

- `flightsim-world::mesh` — 地形メッシュの**データ**生成。**スカート付き**
  （隣接 LOD の隙間から裏や空が見えるのを防ぐ）。GPU には触らない
- `flightsim-core::RenderFrame` — 描画用のローカル接平面（X = 東、Y = 上、Z = 南）。
  Bevy の大気散乱がシェーダ内で `world_position.y` を海抜高度として読むため、
  ECEF 相対では空の色が緯度経度で出鱈目になる
- `flightsim-sim::Simulation` — 1 描画フレームぶんだけ進める逐次 API。
  補間結果は `InterpolatedState` という**別の型**にしてあり、
  誤って物理へ書き戻す経路を型で塞いでいる

### 大気散乱

Bevy 0.18 の Bruneton モデル（Rayleigh + Mie + オゾン、事前計算 LUT）をそのまま使う。
自前実装より engine 提供のほうが正しく、保守もされる。

### 修正 — 目視確認で見つけた 3 件

いずれも**テストは通るのに絵が壊れる**種類。実際に起動して撮らなければ見つからなかった。

- **機体軸 → カメラ軸の回転が逆向き。** 地平線が画面の真ん中に縦に立った。
  しかも**テストの側も逆向きの性質を検査していたため通っていた**。
  検査を「合成した結果どこを向くか」で書き直した
- **地形タイルに回転を与えていなかった。** メッシュの頂点は ECEF 軸なのに
  描画フレームは ENU 軸なので、緯度経度に応じた角度だけ傾く。
  **赤道・本初子午線の近くでは正しく見える**ので気付きにくい
- **光量と露出が噛み合っていなかった。** `FULL_DAYLIGHT` は 2 万 lux だが
  `Exposure::SUNLIGHT` は 10 万 lux 級向け。空だけ明るく地面が真っ黒になった

### CI

純 Rust 側と描画層でジョブを分けた。物理や地形を壊したときに、Bevy のビルドを
待たずに気付けることに価値がある。描画層は**実行しない**（GPU もディスプレイも
無いランナーで走らせても、落ちた原因が環境かコードか切り分けられない）。

### 既知の制限

- **機体の 3D モデルが無い。** 外部視点では機体が映らない
- ゲームパッド／HOTAS 未対応。キーボードのみ
- 地表テクスチャが無い。単色のマテリアル
- 焼いた範囲の外は何も描かれない。地平線まで地形を出すには広域を焼く必要がある

---

M2（描画）に入る前の欠陥掃除と、性能測定の基盤整備。

### 追加

- **ベンチマーク**（criterion、`cargo bench --workspace`）。`flightsim-core` /
  `flightsim-fdm` / `flightsim-world` に配置。dev-dependency なので実行時の
  依存グラフには現れない
- **敵対的テスト 27 件**。極、日付変更線、1 時間飛行、1 タイルも入らないキャッシュ、
  縮退した設定値、海面下・高地の出発点などを突く（`tests/stress.rs`）
- **M2 の予行演習テスト**（`tests/render_rehearsal.rs`）。`FloatingOrigin` /
  `LodSelector` / `StreamingScheduler` は M2 の中核でありながら自クレートの
  単体テスト以外で一度も動いていなかった。実際の飛行軌跡をカメラ軌道にして
  描画フレームの手順を回し、不変条件を検査する
- `&T` に対する `TileSource` 実装。1 つの供給元を複数の `Terrain` で共有できる

### 修正

- **浮いてしまった機体が誰にも管理されない状態になっていた。** 零迎角でも揚力係数は
  正なので、回転速度に達しなくても速度だけで浮く。実測で 146 m まで上昇したが
  フェーズは `TakeoffRoll` のままで、翼は水平固定・高度は無管理だった。
  接地していなければ無条件に上昇フェーズへ移るようにした

### 測定

**60 Hz フレームの予算 16 667 µs に対し、描画より下は全部合わせて約 1%。**

| 項目 | 実測 | フレーム比 |
|---|---:|---:|
| FDM 1 ステップ（空中 / 接地中） | 3.21 µs / 26.5 µs | 0.04% / 0.32% |
| 接地平面 1 回（5 探査） | 311 ns | 0.004% |
| LOD 選択 | 1.5〜2.4 µs | 0.014% |
| floating origin 打ち直し（10 万点） | 61.0 µs | 0.37% |
| タイル復号 65×65 | 10.5 µs | 0.50%（予算 8 枚） |

[ADR-0002](docs/adr/0002-coordinate-system.md) が「フレームスパイクになり得る」と
警告していた floating origin の打ち直しは、**測定の結果 非問題だった**（10 万
オブジェクトで 0.37%）。ADR に測定値を追記して懸念を閉じた。

---

## [0.3.0-alpha.1] — 2026-08-06

**M1 完了。実地形の上をヘッドレスで飛べる。**

### 追加 — `flightsim-sim`（新クレート）

地形と飛行力学を結線する統合層（[ADR-0006](docs/adr/0006-simulation-integration-layer.md)）。
`flightsim-fdm` は `flightsim-world` を参照できないため、その規約を守ったまま
両者を繋ぐ場所として新設した。純 Rust で、Bevy に依存しない。

- **接地平面の生成** — 地形標高と、有限差分で求めた北・東方向の勾配を
  `Environment::with_ground_plane` へ渡す。勾配は NaN を潰し `tan(60°)` でクランプする
- **決定論的フライトディレクタ** — PD 制御。壁時計時間・乱数・積分状態を持たない
  純粋関数なので FDM の決定論を壊さない。**実機の自動操縦の再現ではなく、
  回帰テストの駆動装置**
- **フェーズ駆動の場周飛行** — 離陸滑走 → 上昇 → 巡航 → 旋回 → 進入 → フレア →
  接地 → 減速
- **ヘッドレスランナー `flightsim-headless`** — 軌跡を CSV で出力する CLI

### 追加 — `flightsim-world`

- `terrain` モジュール。焼いたタイルから測地座標の標高を引く。深いレベルから順に
  探し、無ければ粗いレベルへ落ちる
- `DiskTileSource` / `MemoryTileSource` / `Box<dyn TileSource>`
- パスと中身のタイル ID が食い違うファイルを拒否する。使うと**全く違う場所の
  地形が静かに使われる**

### 追加 — `flightsim-tilegen`

- `--min-coverage`。被覆率がこれを下回るタイルを書かない

### 修正

- 依存規約の検査を `flightsim-sim` まで拡張

### 検証

357 件のテストが通る。M1 の完了条件は 14 件の統合テストで検査している。
実測値:

| 項目 | 値 |
|---|---|
| 接地時の沈下率 | 1.86 m/s（設計限界 3 m/s） |
| タイル境界での標高段差 | 最大 0.07 m |
| 接地時の車輪めり込み | 0.028 m（理論値 10 232 N ÷ 360 000 N/m = 0.0284 m と一致） |
| 決定論 | 同一シナリオの 2 回実行がビット単位で一致 |

### 既知の制限

- **fill を含むタイルは実行時から「実データ」と区別できない。** 焼いた範囲の縁を
  飛んだ機体が、実測で 179 m の段差に遭遇した。しかも 1201 サンプル全てで
  「地形データあり」と報告される。`--min-coverage` はこの**嘘を消す**（該当 113
  サンプルが 0 件になる）が、**段差自体は消えない**。データ境界では不可避で、
  呼び出し側が既定値（海面）を決める設計だから
- **衝突判定が無い。** 上昇率を上回る速さで迫り上がる地形に対し、機体は接地したまま
  斜面を引きずられて登る。実測で 200 秒間その状態が続いた
- 場周飛行の計画は地形を考慮しない。山越えのような経路では時間内に完了しない
- フライトディレクタのゲインは `light_single` 向け。機体を増やすたび調整が要る
- 有限差分の勾配は、DEM のバイリニア補間がセル境界で微分不連続なため段階的に変わる

---

## [0.2.0-alpha.1 以降の未リリース分]

### 追加

- **`flightsim-tilegen`** — Copernicus DEM の GeoTIFF から実行時タイルを焼く
  オフライン CLI。EPSG:4326 の単バンド浮動小数点ラスタを読み、地理座標系
  クアッドツリーのタイルへリサンプリングして書き出す
  - 足跡が元画素より粗い場合は**面積平均**、細かい場合はバイリニア補間。
    粗い側で点サンプリングするとエイリアシングが幾何誤差として算出され、
    平野が過剰に細分化される
  - `GTRasterTypeGeoKey` を見て `PixelIsArea` / `PixelIsPoint` を切り替える。
    取り違えると 30 m データで 15 m ずれる
  - 投影座標系のラスタは**度として読まずにエラーにする**。静かに全く違う場所の
    地形になるため
  - 日付変更線をまたぐ範囲（`west > east`）と極を扱う
  - nodata 画素を重みから外し、海岸線が虫食いにならないようにする
- **実行時タイル形式 `.fsdem`**（[ADR-0005](docs/adr/0005-runtime-tile-format.md)）を
  `flightsim-world::dem::io` に実装。`u16` 量子化 + タイル毎スケールで、
  標高差 4 000 m の山岳でも約 6 cm 分解能
  - フォーマット版とペイロードの FNV-1a チェックサムを持つ。
    **壊れたファイル・切り詰め・版違いで panic せずエラーを返す**
  - 幾何誤差をヘッダに埋め、LOD 選択が画素を読まずに判断できる

### 修正

- `scripts/check-architecture.sh` が **`cargo tree` の失敗を握り潰していた**。
  マニフェストに誤りがあると全ての規約検査が「依存ゼロ」として黙って通り、
  安全網が無言で外れる状態だった。事前検査を足して即座に落とすようにした
  （実際にこの穴を踏んで気付いた）

### 検証

297 件のテストが通る。実行時タイル形式については、TIFF 6.0 と ADR-0005 の仕様書だけを
見て Python で独立に writer / reader を書き、Rust 実装と突き合わせた。
同じ実装者の writer と reader を往復させても共通の誤解は検出できないため。

### 既知の制限

- **被覆外を埋めた部分が幾何誤差を押し上げる。** 実データと fill の段差が崖として
  扱われ、実測で被覆完全なタイル最大 10.7 m に対し fill を含むタイルは最大 375.3 m
  になった。実データが無い場所ほど細分化されるという逆転が起きる。
  当面は `--bounds` で被覆内に生成を限定する。将来はフェザリングか、
  被覆率の低いタイルの除外で対処する
- タイル生成は標高のみ。OSM（空港・建物）と地表画像は未対応
- ダウンロード機能は無い。GeoTIFF は手元に用意する必要がある

---

## [0.2.0-alpha.1] — 2026-08-03

### 追加

- `flightsim-fdm` に前脚 1 本・主脚 2 本の着陸装置を追加。ばねダンパ式の接地反力、
  最大ストローク後のバンプストップ、転がり・横滑り摩擦、ホイールブレーキに対応
- 呼び出し側が `Environment` へ地面の楕円体高とローカル勾配を渡す接地平面入力
- 脚の固有振動数を考慮する内部サブステップ判定
- 10 分静止、理論沈み込み、滑走離陸、3 m/s・10 m/s 着地、傾斜面、決定論の回帰テスト

---

## [0.1.0] — 2026-08-01

**M1: 物理と地形の基盤。**

最初のリリース。地球規模フライトシミュレータの土台にあたる部分を、
テストで検証できる状態まで作ったもの。

> **これはまだゲームではありません。** 描画が一切なく、実行できるのは
> テストと診断用の example のみです。何が動いて何が動かないかは
> [ARCHITECTURE.md §7](ARCHITECTURE.md#7-現状のスコープ) を参照してください。

### 追加 — `flightsim-core`

座標系・単位の共通基盤。他のいかなるクレートにも依存しない。

- 単位付き newtype（`Meters` / `Knots` / `Radians` / `Kelvin` ほか）。
  ft・kt・deg の取り違えを型で潰す
- WGS84 測地系と ECEF 変換。ECEF → 測地は Bowring 法 + 固定点反復で、
  地表付近 0.1mm 以下の精度
- ローカル接平面（NED / ENU）と、3-2-1 オイラー角による姿勢。
  ピッチ ±90°（垂直上昇・降下）のジンバルロックを処理する
- Floating origin。`f64` ECEF から描画用 `f32` への変換と、アンカーの打ち直し
- 固定タイムステップのアキュムレータ（death spiral 防止のクランプ付き）

### 追加 — `flightsim-fdm`

6 自由度の飛行力学モデル。決定論的で、Bevy にもワールドにも依存しない。

- ISA 標準大気（幾何高度 -5km〜86km）。ジオポテンシャル高度への換算と、
  非標準日の温度偏差に対応
- WGS84 正規重力（Somigliana の式）。緯度と高度による変化を含む
- 空力係数モデル。失速角の前後で線形モデルと平板理論をシグモイドでブレンドし、
  失速後に揚力が落ち抗力が急増する挙動を再現
- RK4 固定ステップ積分。角速度に応じた内部サブステップ分割付き
- 定出力プロペラモデル（静止推力の上限付き）

### 追加 — `flightsim-world`

地形タイル・LOD・ストリーミング。GPU には触れない。

- 地理座標系クアッドツリー（Cesium geographic tiling scheme 互換）。
  Web メルカトルと違い極まで表現できる
- DEM のバイリニアサンプリングと、実データから算出する幾何誤差
- 幾何誤差ベースの screen-space error による LOD 選択。距離ベースではない
- 距離優先の読み込みキューと、バイト数上限つき LRU キャッシュ。
  1 フレームあたりの読み込み数に上限を設けフレームスパイクを防ぐ

### 追加 — 開発基盤

- 意思決定ログ [ADR-0001〜0004](docs/adr/)。エンジン選定、座標系、
  地形データ方針、シミュレーションループの根拠とトレードオフ
- 8 つの専門エージェント定義（[.claude/agents/](.claude/agents/)）
- CI: `cargo test` / `clippy -D warnings` / `fmt --check` / `doc -D warnings`、
  および[依存規約の機械検査](scripts/check-architecture.sh)
- 診断用 example [`aero_trace`](crates/flightsim-fdm/examples/aero_trace.rs)

### 検証

202 件のテスト（ユニット 182、統合 17、doctest 3）が通る。
`core` / `fdm` / `world` は Bevy 非依存のため、GUI もアセットもなしに数秒で完了する。

外部の公表値との照合を重視している。ISA 標準大気の表、WGS84 の定義値と正規重力の
公表値、国際フィート・海里の定義値など。「実装がこう返すから正しい」というテストは
検証にならないため。

### 含まれないもの

- **描画**。大気散乱・地形メッシュ・HUD いずれも未実装
- **実地形データの読み込み**。タイル生成パイプラインが無いため、
  テストは全て合成データで動いている
- 着陸装置と接地反力。したがって離着陸はできない
- FDM とワールドの結線。個別には動くが、地形標高はまだ FDM に渡されていない
- 天候、ライブ交通、オンライン共有ワールド、複数機体、コックピット操作

### 既知の制限

- **コリオリ力・遠心力を無視している。** 巡航速度 250 m/s・緯度 45° で
  重力の約 0.26%。長距離巡航の航法誤差として効くため、
  [ADR-0002](docs/adr/0002-coordinate-system.md) に将来の対応方針を記録してある
- 空力係数は軽single機の代表値であり、**特定の実在機の認証データではない**
- 推力線オフセットによるピッチモーメントが未実装
- 高度は楕円体高であり、ジオイド高（平均海面基準）ではない。
  両者は場所により最大 100m 程度ずれる
- ベンチマークが未整備。性能について測定に基づく主張ができない

[Unreleased]: https://github.com/Xenoah/flightsim-claude/compare/v0.3.0-alpha.1...HEAD
[0.3.0-alpha.1]: https://github.com/Xenoah/flightsim-claude/compare/v0.2.0-alpha.1...v0.3.0-alpha.1
[0.2.0-alpha.1]: https://github.com/Xenoah/flightsim-claude/compare/v0.1.0...v0.2.0-alpha.1
[0.1.0]: https://github.com/Xenoah/flightsim-claude/releases/tag/v0.1.0
