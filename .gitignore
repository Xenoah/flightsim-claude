/target/
**/*.rs.bk

# 生成されたタイル・DEM キャッシュ。数百 GB になり得るのでコミットしない。
/data/cache/
/data/tiles/
/assets/generated/
# Meshy で生成した機体モデル。**権利の扱いがプランに依存する**ので、
# 確認が済むまでコミットしない（ATTRIBUTION.md を参照）。
/assets/aircraft/

# API キー。**中身を絶対にコミットしない。**
# 書き方は .env.example を参照（そちらは追跡する）。
.env
/config/secrets.toml

# IDE
.vscode/
.idea/
*.iml

# Windows
Thumbs.db
desktop.ini
